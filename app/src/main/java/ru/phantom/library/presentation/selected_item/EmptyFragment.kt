package ru.phantom.library.presentation.selected_item

import androidx.fragment.app.Fragment
import ru.phantom.library.R

/**
 * Тут никаких действий не совершается, поэтому ничего и не переопределял
 */
class EmptyFragment : Fragment(R.layout.empty_fragment)